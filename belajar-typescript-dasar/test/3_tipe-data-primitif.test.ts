describe("data type", () => {
  it("should declare", () => {
    const name: string = "eko";
    const balance: number = 1000;
    const isVip: boolean = true;

    console.info(name);
    console.info(balance);
    console.info(isVip);
  });
});
